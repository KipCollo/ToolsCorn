# Introduction

SQL was originally intended to be a data sublanguage specifically.However, with addition in 1996 of the SQL Persistent Stored Module feature (SQL/PSM),the standard became computationally complet.
SQL uses the term `table` in both of terms `relation` and `relvar`, and `row` and `column` in place of `tuple` and `attribute`, respectively.